# Scans are personal documents -- never commit them. Output normally goes to
# ~/Pictures/Scans; this only matters if you point --out back into the repo.
scans/

# User config: printer address differs per machine and network.
printer.env

.DS_Store
